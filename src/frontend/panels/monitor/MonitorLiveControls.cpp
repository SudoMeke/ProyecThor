// El monitor "PGM"/Live (video + transporte + VU meters del player general)
// se movio de Monitor a ViewPanel (ver ViewPanel::RenderLiveTransport) —
// Monitor se quedaba sin aire en pantallas chicas. Este archivo se deja
// vacio (en vez de borrado) porque el editor lo restaura solo desde git al
// borrarlo por disco mientras esta abierto en una pestaña.
