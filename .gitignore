# ── Carpetas de build (CMake, Ninja, makepkg, MSVC) ──────────────────────────
build/
build*/
pkg/
CMakeFiles/
_deps/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake
Makefile
generated/

# ── Instaladores / paquetes generados ────────────────────────────────────────
iss/installer_output/
*.exe
*.obj
*.pkg.tar.zst
*.pkg.tar.xz
*.deb
*.rpm
*.log
*.msi
*.wixpdb

# ── Carpetas de distribución alternativa (si no van en el repo principal) ───
web/
lite/
FoudreVue/
# ── Copia suelta de shaders en la raiz (leftover de builds viejos; el
# codigo real vive en src/backend/shaders/, este NO) ─────────────────────────
/shaders/

# ── Recursos pesados que se distribuyen aparte, no en git ────────────────────
extrabuild/

# ── Notas personales ──────────────────────────────────────────────────────────
src/notes.txt
text.txt

# ── IDEs / editores ───────────────────────────────────────────────────────────
.vscode/
.idea/
*.user

# ── Configuracion local del usuario (puede contener API keys, ej. Pexels) ────
settings.json