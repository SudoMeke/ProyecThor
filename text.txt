- No es el mismo ico
- No se crea carpeta backgrounds

{
    "version": "2.0.0",
    "inputs": [
        {
            "id": "platform",
            "type": "pickString",
            "description": "¿Compilar para qué plataforma?",
            "options": ["linux", "windows", "windows-run"],
            "default": "linux"
        }
    ],
    "tasks": [
        {
            "label": "Build ProyecThor",
            "type": "shell",
            "command": "${workspaceFolder}/scripts/build.sh",
            "args": ["${input:platform}"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        },
        {
            "label": "Run ProyecThor (Wine)",
            "type": "shell",
            "command": "wine",
            "args": ["ProyecThor.exe"],
            "options": {
                "cwd": "${workspaceFolder}/build-win"
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        }
    ]
}

tasks

launch

{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ProyecThor (Linux)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build-linux/ProyecThor",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build-linux",
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                { "description": "Enable pretty-printing", "text": "-enable-pretty-printing", "ignoreFailures": true }
            ],
            "preLaunchTask": "Build ProyecThor"
        }
    ]
}